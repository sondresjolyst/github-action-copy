name: Test
on:
  workflow_run:
    workflows: [Migrate cluster, migrate]
    types: [completed]
    branches: [new-cluster]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Copy cluster
        run: echo "test"
